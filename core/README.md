# CPU Core (`core/`)

This file is now only a compatibility pointer.

The CPU/core overview that used to live here has been merged into the repository
root [`README.md`](../README.md), which is now the canonical place for:
- core scope and model coverage
- MMU/addressing policy
- recent core compatibility notes
- core-level test entry points
- the detailed CPU compliance report

Machine integration, device behavior, CLI options, and boot/media workflows are
documented in [`lsi11/README.md`](../lsi11/README.md).
