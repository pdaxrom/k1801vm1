Place MK-90 removable media images here.

Expected filenames:

- `smp0.bin`
- `smp1.bin`

Optional additional image:

- `trex.bin` is a bootable SMP image taken from the
  [azya52/MK90](https://github.com/azya52/MK90) repository.

These media images are tracked in git for `mk90`.
